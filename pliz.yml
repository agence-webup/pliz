config_files:
  .env.example: .env
  docker_ports.sample.yml: docker_ports.yml

enabled_tasks:
  - npm
  - bower

default:
  src-prepare:
    commands:
      - ["echo", "bonjour les petits éléphants!"]
      -
        - sh
        - "-c"
        - nom="Bruno";
          if [ $nom = "Bruno" ];
          then
            echo "Salut Bruno !";
          fi;
  install:
    commands:
      - ["echo", "Installation en cours..."]

custom:
  - name: custom_example
    commands:
      - ["echo", "Action custom"]
